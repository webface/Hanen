(function(){

    /*
     * Declaration of main angular module for this appllication.
     *
     * It is named articles and has no dependencies (hence the 
     * empty array as the second argument)
     */
    angular
        .module("articles", ['ngStorage']);

})();